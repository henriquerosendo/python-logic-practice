# 🔹 Exercício 3 — Usando filter()

# Dada uma lista de números, filtre apenas os pares usando filter() e lambda.
numeros = [10, 15, 20, 25, 30, 35]
pares = list(filter(lambda x: x % 2 == 0, numeros))
print(pares)   
# Fim do código
